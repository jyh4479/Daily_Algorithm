타겟넘버 - 기존 방문을 체크하는 (visit을 사용하는)기본적인 방법이 아니라 조금 더 쉬운 방법을 생각하는 연습하기

네트워크 - 다른 사람들 방법도 참고해보기

단어변환 - 어... 왜 돌아가지? (다시 복습하기 - 문제 이해가 좀 힘들었음) --> DFS보다 BFS가 더 적당해보임

여행경로 - 조건에 맞는 list를 append해서 사용하려고 했는데 이는 값을 전달하는 것이 아니라 주소를 전달해서 함수를 이동하며 계속 값이 바뀌는 현상이 있었다.
값을 따로 copy=list(해당배열)이런식으로 복사한 후 append해서 문제를 해결하였다 --> 이문제도 DFS보다 BFS가 더 적절해보인다 다시 풀어보기